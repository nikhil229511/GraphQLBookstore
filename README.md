# GraphQLBookstore
This bookstore uses GraphQL as a intermediate between mongodb(database) and the Angular(front-end) keeping API in the NodeJs. This repository contains code that was build just for learning purpose.
